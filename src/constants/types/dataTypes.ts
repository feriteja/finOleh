type item = {
  name: string;
  price: number;
  originalPrice: number;
  img: string;
  shop: {
    lat: number;
    location: string;
    name: string;
    lng: number;
  };
  ref: string;
};

export {item};
